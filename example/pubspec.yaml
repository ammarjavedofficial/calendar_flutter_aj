name: calender_flutter_aj
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  calender_flutter_aj:
  # When depending on this package from a real application you should use:
  #   calender_flutter_aj : ^x.y.z    

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true